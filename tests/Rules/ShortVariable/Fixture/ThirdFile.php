<?php

class ThirdFile
{
    public function testMethod()
    {
        $i = 'third file variable';
        $x = 'third file x';

        $items = [1, 2, 3];

        foreach ($items as $k => $v) {
            // Loop body
        }

        $k = 'after foreach';
        $v = 'after foreach';
    }
}
