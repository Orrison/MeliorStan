includes:
    - ../../../../config/extension.neon

rules:
    - Orrison\MeliorStan\Rules\ShortVariable\ShortVariableRule

parameters:
    meliorstan:
        short_variable:
            exceptions: ['i', 'j', 'k', 'x', 'y', 'e']
